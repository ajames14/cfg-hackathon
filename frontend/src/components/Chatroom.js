import React from 'react'

/*******
 * Shows number of users or user icons
 * GET users
 * GET posts
 * GET comments
 * Map through to get all posts
 * If no posts or user == 1 show text saying something like 'welcome, be the first to post'
 * *******/

const Chatroom = () => {
  return (
    <div className="section">
      <div className="container">
        <div className="title">Welcome to Food Swap</div>
      </div>
    </div>
  )
}

export default Chatroom
